%List of globals
global MainHandle; %stores the handle for MainGUI
global AboutHandle; %stores the handle for AboutGUI
global ScreenSize;  %Screen size array ---done
global DefaultAspectRatio;  %Default aspect ratio for reference ---done
global MainFigurePosition;  %Main figure size
global CurrentLayout;   %Current active layout ---done
global Timer;   %Timer ---done
global TimeScale;   %Time scale ---done


global ApplicationState; %Keeps a track of app state, -1, After quit, 0 - Idle, 1 - Running, 2 - Paused

global CurrentSimulation; %Keeps a track of currently opened Simulation

global WaveInC1;    %C1 for wave
global WaveInC2;    %C2 for wave
global WaveInC3;    %C3 for wave

global WaveInC1Priority;    %C1 for wave priority
global WaveInC2Priority;    %C2 for wave priority
global WaveInC3Priority;    %C3 for wave priority

global LineGraph1;  %store the line output of graph 1
global LineGraph2;  %store the line output of graph 2
global LineGraph3;  %store the line output of graph 3

global SurfaceGraph1;   %stores the surface output of graph 1
global SurfaceGraph2;   %stores the surface output of graph 2
global SurfaceGraph3;   %stores the surface output of graph 3

global OtherSliderValue1;   %value of the first other slider
global OtherSliderValue2;   %value of the second other slider
global OtherSliderValue3;   %value of the third other slider






